{
  "$id": "https://github.com/KirPonomarev/dual-contour-research-os/contracts/v1/ApprovalReceipt.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "classification": {
      "enum": [
        "D0_PUBLIC",
        "D1_INTERNAL_SANITIZED",
        "D2_DOMAIN_CONFIDENTIAL",
        "D3_RESTRICTED"
      ]
    },
    "contour": {
      "enum": [
        "bridge",
        "market",
        "security",
        "governance"
      ]
    },
    "integrity": {
      "additionalProperties": false,
      "properties": {
        "parent_refs": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "payload_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "payload_sha256",
        "parent_refs"
      ],
      "type": "object"
    },
    "issued_at": {
      "format": "date-time",
      "type": "string"
    },
    "issuer": {
      "additionalProperties": false,
      "properties": {
        "authority_class": {
          "minLength": 1,
          "type": "string"
        },
        "id": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "id",
        "authority_class"
      ],
      "type": "object"
    },
    "object_id": {
      "minLength": 1,
      "type": "string"
    },
    "payload": {
      "additionalProperties": false,
      "properties": {
        "action_class": {
          "minLength": 1,
          "type": "string"
        },
        "expires_at": {
          "minLength": 1,
          "type": "string"
        },
        "job_spec_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "nonce": {
          "minLength": 1,
          "type": "string"
        },
        "policy_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "protocol_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "quotas": {
          "type": "object"
        },
        "revoked": {
          "type": "boolean"
        },
        "stop_conditions": {
          "type": "array"
        }
      },
      "required": [
        "action_class",
        "job_spec_sha256",
        "protocol_sha256",
        "policy_sha256",
        "quotas",
        "stop_conditions",
        "expires_at",
        "nonce",
        "revoked"
      ],
      "type": "object"
    },
    "schema_id": {
      "const": "ApprovalReceipt"
    },
    "schema_version": {
      "const": "1.0.0"
    }
  },
  "required": [
    "schema_id",
    "schema_version",
    "object_id",
    "issued_at",
    "issuer",
    "contour",
    "classification",
    "payload",
    "integrity"
  ],
  "title": "ApprovalReceipt",
  "type": "object",
  "x-authority": "human-approval",
  "x-owner": "governance",
  "x-writer": "operator-authority"
}
