{
  "$id": "https://github.com/KirPonomarev/dual-contour-research-os/contracts/v1/ReleaseManifest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "classification": {
      "enum": [
        "D0_PUBLIC",
        "D1_INTERNAL_SANITIZED",
        "D2_DOMAIN_CONFIDENTIAL",
        "D3_RESTRICTED"
      ]
    },
    "contour": {
      "enum": [
        "bridge",
        "market",
        "security",
        "governance"
      ]
    },
    "integrity": {
      "additionalProperties": false,
      "properties": {
        "parent_refs": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "payload_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "payload_sha256",
        "parent_refs"
      ],
      "type": "object"
    },
    "issued_at": {
      "format": "date-time",
      "type": "string"
    },
    "issuer": {
      "additionalProperties": false,
      "properties": {
        "authority_class": {
          "minLength": 1,
          "type": "string"
        },
        "id": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "id",
        "authority_class"
      ],
      "type": "object"
    },
    "object_id": {
      "minLength": 1,
      "type": "string"
    },
    "payload": {
      "additionalProperties": false,
      "properties": {
        "config_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "dependency_lock_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "image_digests": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "policy_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        },
        "previous_release_ref": {
          "minLength": 1,
          "type": "string"
        },
        "release_sha": {
          "minLength": 1,
          "type": "string"
        },
        "sbom_ref": {
          "minLength": 1,
          "type": "string"
        },
        "schema_sha256": {
          "pattern": "^[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "release_sha",
        "image_digests",
        "policy_sha256",
        "config_sha256",
        "schema_sha256",
        "dependency_lock_sha256",
        "sbom_ref",
        "previous_release_ref"
      ],
      "type": "object"
    },
    "schema_id": {
      "const": "ReleaseManifest"
    },
    "schema_version": {
      "const": "1.0.0"
    }
  },
  "required": [
    "schema_id",
    "schema_version",
    "object_id",
    "issued_at",
    "issuer",
    "contour",
    "classification",
    "payload",
    "integrity"
  ],
  "title": "ReleaseManifest",
  "type": "object",
  "x-authority": "immutable-release",
  "x-owner": "governance",
  "x-writer": "agent-0"
}
